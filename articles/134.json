{
  "categoryId": 44,
  "categoryName": "学习笔记",
  "content": "## 一、三种变量声明方式\n\n### 1.1 `var` + 类型（最正式）\n\n```go\nvar name string = \"pc\"\nvar age int = 25\n```\n\n**适用场景：**\n- 声明包级变量（函数外面）\n- 需要明确指定类型\n- 希望代码读起来一目了然\n\n### 1.2 `var` + 类型推断（省去类型）\n\n```go\nvar name = \"pc\"   // Go 自动推断为 string\nvar age = 25      // 自动推断为 int\n```\n\nGo 会根据右边的值自动推断类型，不用每次都写类型。\n\n### 1.3 `:=` 短声明（最常用，函数内专用）\n\n```go\nname := \"pc\"\nage := 25\n```\n\n**特点：**\n- **只能在函数内部使用**\n- 最简洁，日常写代码最常用\n- 可以配合多返回值：`count, err := doSomething()`\n- `:=` 允许**混合使用**新变量和已有变量：\n\n```go\ncount, err := doSomething()    // count 和 err 都是新的\ntotal, err := doNext()         // err 已存在，只是重新赋值；total 是新变量\n```\n\n### 对比总结\n\n| 写法 | 位置限制 | 可读性 | 使用频率 |\n|:-----|:--------|:------|:--------|\n| `var x int = 10` | 无限制 | ⭐⭐⭐ 最高 | 包级变量 / 明确类型 |\n| `var x = 10` | 无限制 | ⭐⭐ | 包级变量 |\n| `x := 10` | **仅函数内** | ⭐ 最简洁 | **日常最高** ✅ |\n\n---\n\n## 二、Go 的基本类型\n\n### 2.1 数值类型\n\n```go\nvar (\n    a int     = 42     // 平台相关：32位或64位\n    b int8    = 127    // -128 ~ 127\n    c int16   = 32767  // -32768 ~ 32767\n    d int32   = 20     // -21亿 ~ 21亿\n    e int64   = 100    // 很大的数\n    \n    f uint    = 100    // 无符号整数\n    g byte    = 'A'    // byte = uint8 的别名，值 = 65\n)\n```\n\n**浮点数：**\n\n```go\nvar (\n    price float32 = 19.9        // 精度较低\n    pi    float64 = 3.1415926   // 精度高，推荐使用 ✅\n)\n```\n\n> 💡 **原则：整数用 `int`，小数用 `float64`，除非有特殊需求。**\n\n### 2.2 布尔类型\n\n```go\nvar enabled bool = true\nvar disabled bool = false\n```\n\n布尔值只能为 `true` 或 `false`，**不能**像 Python 那样用 `0` / `1` 代替。\n\n### 2.3 字符串类型\n\n```go\nvar name string = \"Go语言\"\n\n// 字符串可以拼接\ngreeting := \"你好，\" + \"Go\"\n\n// 取长度\nfmt.Println(len(\"Go\"))        // 2\n\n// 索引取值（拿到的是 byte）\nfmt.Println(\"Go\"[0])          // 71（'G' 的 ASCII）\n```\n\n> ⚠️ 注意：Go 字符串的 `len()` 返回的是**字节数**，不是字符数。中文等 Unicode 字符可能占多个字节。\n\n### 2.4 零值（默认值）\n\nGo 的每个变量都有默认值，**不存在\"未初始化\"的情况**：\n\n| 类型 | 零值 |\n|:-----|:-----|\n| `int` / 所有数值类型 | `0` |\n| `float64` | `0.0` |\n| `bool` | `false` |\n| `string` | `\"\"`（空字符串） |\n| 指针 / 接口 / 切片 / map | `nil` |\n\n```go\nvar num int        // num = 0，不需要写 = 0\nvar str string     // str = \"\"，可以直接用\n```\n\n---\n\n## 三、批量声明\n\n当有多个变量时，可以用 `var()` 块批量声明，让代码更整洁：\n\n```go\nvar (\n    name    string = \"pc\"\n    age     int    = 25\n    enabled bool   = true\n    score   int           // 不赋值，默认 0\n)\n```\n\n---\n\n## 四、值得注意的细节\n\n**1. Go 不允许声明了变量却不使用**\n\n```go\na := 1\nb := 2\nfmt.Println(a)   // ❌ 编译错误：b declared but not used\n```\n\n这是 Go 的设计哲学——**代码必须干净**。不用的变量就删掉或用 `_` 忽略。\n\n**2. `=` 与 `:=` 的区别**\n\n```go\nx := 10    // := 声明 + 赋值（变量不存在时使用）\nx = 20     // =  只赋值（变量已存在时使用）\n```\n\n---\n\n## 五、小结\n\n| 概念 | 要点 |\n|:-----|:------|\n| 声明方式 | `var x T = v` / `var x = v` / `x := v` |\n| 函数内 | 优先用 `:=` |\n| 函数外 | 只能用 `var` |\n| 基本类型 | int、float64、bool、string、byte |\n| 零值 | 每个变量都有默认值，不会为空 |\n| 未使用 | Go 编译不通过，必须用掉或删掉 |\n",
  "coverImage": "/data/media/708.png",
  "createdAt": "2026-07-20T10:41:02",
  "id": 134,
  "isPinned": 0,
  "isPublished": 1,
  "next": {
    "id": 135,
    "title": "Go学习笔记：包管理与项目组织——模块、导入导出与 init 执行顺序"
  },
  "prev": {
    "id": 133,
    "title": "Go学习笔记：基本概念与项目结构——GOPATH、Go Modules 与常用命令"
  },
  "series": "Go学习笔记",
  "summary": "Go 的变量声明有三种方式：var + 类型、var 类型推断、:= 短声明。每种方式用在什么场景？int、float64、bool、string 等基本类型怎么用？一篇理清。",
  "tags": [
    {
      "id": 79,
      "name": "Go"
    }
  ],
  "title": "Go学习笔记：变量声明与基本类型——三种写法一次搞懂",
  "updateTime": "2026-07-22T10:41:02.433867",
  "viewCount": 0
}