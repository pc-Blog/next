1:"$Sreact.fragment"
2:I[17467,["/_next/static/chunks/06jjoo_8xgzyj.js","/_next/static/chunks/00ptu9dp.8ona.js","/_next/static/chunks/10odz-6irn5el.js","/_next/static/chunks/09k2slgobs143.js","/_next/static/chunks/09r4hbfw4h9o_.js","/_next/static/chunks/0ak3whllmln1c.js","/_next/static/chunks/116yp~mrtmvyn.js","/_next/static/chunks/05hxwoutgi7z8.js"],"default"]
9:I[97367,["/_next/static/chunks/06jjoo_8xgzyj.js","/_next/static/chunks/00ptu9dp.8ona.js","/_next/static/chunks/10odz-6irn5el.js","/_next/static/chunks/09k2slgobs143.js"],"OutletBoundary"]
a:"$Sreact.suspense"
4:T2cdf,## 一、什么是 K8s？

**Kubernetes（简称 K8s）** 是一个开源的容器编排平台，最初由 Google 设计并捐赠给 Cloud Native Computing Foundation（CNCF）管理。它用于自动化容器化应用的部署、扩缩容和管理。

K8s 的核心能力：

- **服务发现与负载均衡**：自动为容器分配 IP 和 DNS 名，并分发流量
- **自动修复**：容器挂了自动重启，节点挂了自动迁移 Pod
- **自动扩缩容**：根据 CPU/内存使用率自动调整容器数量
- **存储编排**：自动挂载本地或云存储
- **滚动更新与回滚**：逐个替换容器实例，更新失败自动回退

---

## 二、有了 Docker，为什么还要用 K8s？

Docker 和 K8s 解决的是不同层面问题，两者互补：

| 需求 | Docker | K8s |
|------|--------|-----|
| 单个容器的构建和运行 | ✅ 原生支持 | ❌ 不负责构建 |
| 多个容器的编排调度 | ❌ 需手动管理 | ✅ 声明式自动管理 |
| 容器崩溃后自动恢复 | ❌ 不复原 | ✅ 自动重启 Pod |
| 流量波动时自动扩容 | ❌ 手动操作 | ✅ 支持 Horizontal Pod Autoscaler |
| 发布新版本无停机 | ❌ 需手动编排 | ✅ 支持 Rolling Update |
| 跨机器的集群管理 | ❌ 单机为主 | ✅ 天然支持集群 |

在实际项目中，通常用 **Docker 构建镜像**，用 **K8s 运行和管理容器**，两者配合使用。Docker Desktop 内置了 K8s，开发者可以在本地一键启动一个单节点 K8s 集群进行开发和测试。

---

## 三、环境说明

本文的环境如下：

```
宿主机：Windows 11
WSL 2：Ubuntu 22.04
Docker Desktop：已安装，启用 WSL 2 后端
FinalShell：SSH 连接工具
```

> ⚠️ 如果你用的是真实的 Linux 系统或传统虚拟机（如 VMware、VirtualBox），操作步骤会有所不同。本文基于 **WSL + Docker Desktop** 的组合，K8s 集群运行在 Windows 宿主机上，而 `kubectl` 命令在 WSL Ubuntu 中执行，因此需要额外配置网络互通。

---

## 四、在 Docker Desktop 中启用 K8s

### 4.1 确认 Docker Desktop 正常运行

在 PowerShell 中执行：

```powershell
docker version
```

确认 Docker Engine 和 Client 都有版本信息输出。

### 4.2 启用 Kubernetes

1. 打开 **Docker Desktop**
2. 点击左侧导航栏的 **Kubernetes** 图标（☸️）
3. 点击 **"Create cluster"** 按钮
4. 配置集群参数：
   - **Provisioning method**：选择 `kubeadm` 或 `kind`（详见下方对比）
   - **Kubernetes version**：选择当前最新稳定版（如 v1.35）
   - **Nodes**：1（单节点已涵盖运行应用所需的所有组件）
5. 点击 **Create**，等待 1~2 分钟创建完成

> 也可以选择 `kind` 预配器，支持多节点集群（1 control-plane + N worker），资源消耗更大，适合学习 Pod 调度原理，初学阶段用 kubeadm 即可。

两种模式的对比如下：

| 特性 | kubeadm 模式 | kind 模式 |
|------|-------------|----------|
| 节点名 | `docker-desktop` | `desktop-control-plane` |
| API Server 地址 | `kubernetes.docker.internal:6443`（固定） | `127.0.0.1:XXXXX`（随机端口，重启会变） |
| Docker Desktop 重启后数据 | 保留 ✅ | 丢失 ❌ |
| LoadBalancer 端口映射 | ❌ 只映射 80 端口 | ✅ 所有端口都自动映射 |
| 最大节点数 | 单节点 | 支持多节点 |
| 适用场景 | 日常开发、数据持久化 | 多服务 LoadBalancer、学习调度原理 |

> ⚠️ **重要提醒**：两种模式各有优劣，根据需求选择：
> - **`kubeadm` 模式**：重启后数据保留，API 地址固定。但 LoadBalancer **只映射 80 端口**，多个服务需要使用 `port-forward` 或 Ingress 等方式访问
> - **`kind` 模式**：重启后数据会丢失，端口会变化。但 LoadBalancer **所有端口都能映射**，多服务部署更灵活，配合 `cloud-provider-kind` 自动创建代理容器
> 
> 如果你主要用 `port-forward` 访问服务，选 kubeadm 更省心；如果你要用 LoadBalancer 暴露多个服务端口，kind 更合适。

![Docker Desktop Kubernetes 视图](/data/media/545.png)

创建完成后，Docker Desktop 的 Kubernetes 视图中可以看到集群的运行状态：

![集群运行状态](/data/media/546.png)

---

## 五、Node（节点）的概念

**Node（节点）** 是 K8s 集群中的工作机器，可以是物理机或虚拟机，负责运行容器化应用。

K8s 集群由两种类型的节点组成：

| 节点类型 | 职责 | 组件 |
|---------|------|------|
| **Control Plane（控制面）** | 管理整个集群，做出全局决策 | API Server、Scheduler、Controller Manager、etcd |
| **Worker Node（工作节点）** | 实际运行应用容器 | kubelet、kube-proxy、容器运行时 |

在 Docker Desktop 的单节点模式下，control-plane 同时也具备 worker 的功能，可以直接调度 Pod。

通过以下命令查看集群中的节点：

```bash
kubectl get nodes
```

输出示例（kubeadm 模式）：

```
NAME             STATUS   ROLES           AGE     VERSION
docker-desktop   Ready    control-plane   10m     v1.34.1
```

- `STATUS` 为 **Ready** 表示节点正常运行
- `ROLES` 为 **control-plane** 表示它是管理节点
- `VERSION` 显示 K8s 版本号

---

## 六、配置 kubectl（关键步骤）

### 6.1 什么是 kubectl？

**kubectl** 是 K8s 的命令行客户端工具，通过调用 K8s API Server 来管理集群资源（节点、Pod、Service 等）。所有对集群的操作几乎都可以通过 `kubectl` 完成。

### 6.2 环境问题：WSL 无法直连 K8s API

由于 K8s 集群运行在 **Windows 宿主机** 的 Docker Desktop 中，而 `kubectl` 在 **WSL Ubuntu 子系统** 中执行，两个环境网络隔离：

```
WSL Ubuntu 中的 kubectl → 127.0.0.1:6443 → WSL 自己的 loopback ❌
Windows PowerShell 中的 kubectl → 127.0.0.1:6443 → Windows 的 loopback ✅
```

直接运行会报错：

```
The connection to the server kubernetes.docker.internal:6443 was refused
```

解决需要 **两步**：打通网络 + 同步配置。

### 6.3 第一步：打通网络（永久方案）

编辑 Windows 上的 `.wslconfig` 文件（路径：`C:\Users\<你的用户名>\.wslconfig`），写入：

```ini
[wsl2]
networkingMode=mirrored
```

**`networkingMode=mirrored`**（镜像网络模式）让 WSL 2 与 Windows 共享网络命名空间，WSL 中的 `127.0.0.1` 将直接指向 Windows 宿主机。该配置修改一次永久生效。

使配置生效：

```powershell
# PowerShell 中执行
wsl --shutdown
```

重新打开 WSL Ubuntu 即可。

### 6.4 第二步：同步 kubeconfig（通用方案）

Docker Desktop 启用 K8s 后，会在 Windows 侧自动生成正确的 K8s 连接配置（`C:\Users\<用户名>\.kube\config`），但 WSL 中不会自动同步。需要手动复制。

先在 **Windows PowerShell** 中确认 K8s 能正常连接：

```powershell
kubectl get nodes
```

确认输出正常后，到 **WSL Ubuntu** 中执行：

```bash
# 1. 确认 kubectl 是否已安装
kubectl version --client

# 如果没有安装，则下载
curl -LO "https://dl.k8s.io/release/v1.35.0/bin/linux/amd64/kubectl"
chmod +x kubectl
sudo mv kubectl /usr/local/bin/

# 2. 创建 .kube 目录（如果不存在）
mkdir -p /root/.kube

# 3. 复制 Windows 的 K8s 配置到 WSL
cp /mnt/c/Users/<你的Windows用户名>/.kube/config /root/.kube/config
```

> 如果 WSL 使用普通用户而非 root，将配置复制到 `/home/<你的WSL用户名>/.kube/config`

### 6.5 验证连接

```bash
kubectl get nodes
```

成功输出：

```
NAME             STATUS   ROLES           AGE     VERSION
docker-desktop   Ready    control-plane   10m     v1.34.1
```

![验证 kubectl 连接](/data/media/547.png)

---

## 七、部署第一个应用

环境配置完成，接下来部署一个 **Nginx** Web 服务器，验证集群能正常工作。

### 7.1 创建 Deployment

Deployment 是 K8s 中最常用的资源类型，用于声明式地管理 Pod：

```bash
kubectl create deployment k8s-test --image=nginx
```

- `k8s-test`：Deployment 的名称
- `--image=nginx`：使用的容器镜像（从 Docker Hub 拉取）

K8s 会自动拉取 Nginx 镜像，创建一个 Pod 并在节点上启动容器。

### 7.2 查看 Pod 状态

```bash
kubectl get pods
```

输出示例：

```
NAME                        READY   STATUS    RESTARTS   AGE
k8s-test-66588d67cf-stvgs   1/1     Running   0          66s
```

- `READY 1/1`：Pod 中 1 个容器，1 个就绪
- `STATUS Running`：正常运行
- `RESTARTS 0`：未发生重启

如果状态为 `ContainerCreating` 或 `Pending`，等待几秒再次检查即可。

### 7.3 查看完整资源状态

```bash
kubectl get pods,svc
```

`svc` 是 Service 的缩写，Service 为 Pod 提供一个稳定的入口地址。刚创建时只有默认的 `kubernetes` 服务。

### 7.4 访问 Nginx（端口转发）

因为 Pod 运行在 K8s 集群内部网络，不能直接从宿主机访问。使用 `port-forward` 将本地端口映射到 Pod 端口：

```bash
kubectl port-forward deployment/k8s-test 8080:80
```

这条命令将本地 `8080` 端口的流量转发到 `k8s-test` 的 `80` 端口。

保持终端窗口打开，在浏览器访问 **http://localhost:8080**，出现 Nginx 欢迎页面即部署成功：

![Nginx 欢迎页](/data/media/548.png)

### 7.5 查看 Pod 日志

```bash
kubectl logs k8s-test-66588d67cf-stvgs
```

可以看到 Nginx 的访问日志，便于调试。

### 7.6 清理测试资源

```bash
kubectl delete deployment k8s-test
```

删除后，K8s 会自动终止并清除对应的 Pod。

---

## 八、常用命令速查

| 命令 | 作用 |
|------|------|
| `kubectl get nodes` | 查看所有节点 |
| `kubectl get pods` | 查看当前命名空间的所有 Pod |
| `kubectl get pods -n kube-system` | 查看 K8s 系统组件（CoreDNS、API Server 等） |
| `kubectl get svc` | 查看所有 Service |
| `kubectl get all` | 查看当前命名空间的所有资源 |
| `kubectl create deployment <名称> --image=<镜像>` | 创建 Deployment |
| `kubectl port-forward deployment/<名称> 本地端口:容器端口` | 将本地端口转发到 Pod |
| `kubectl delete deployment <名称>` | 删除 Deployment |
| `kubectl logs <Pod名称>` | 查看 Pod 日志 |
| `kubectl describe pod <Pod名称>` | 查看 Pod 详细信息（事件、状态等） |
| `kubectl get events` | 查看集群事件（排错常用） |

---

## 九、总结

本文从零开始，在 Docker Desktop 上完成了 K8s 的启用、配置和部署验证：

| 步骤 | 操作 | 验证方式 |
|------|------|---------|
| 启用 K8s | Docker Desktop → Kubernetes → Create cluster | 界面显示集群运行中 |
| 打通网络 | 配置 `.wslconfig` 启用镜像网络 | WSL 中 `127.0.0.1` 指向 Windows |
| 安装 kubectl | 下载 kubectl 二进制文件 | `kubectl version --client` |
| 同步配置 | 复制 Windows 的 kubeconfig 到 WSL | `kubectl get nodes` 返回节点信息 |
| 部署应用 | `kubectl create deployment` | `kubectl get pods` 显示 Running |
| 访问验证 | `kubectl port-forward` | 浏览器访问 localhost:8080 |

以上步骤验证了 Docker Desktop 内置的 K8s 集群可以正常使用。基于这个环境，可以进一步学习：

- 编写 YAML 配置文件实现声明式管理
- 使用多节点 kind 集群
- 部署完整的 Web + 数据库应用
- 学习 ConfigMap、Secret、Ingress 等高级资源
0:{"rsc":["$","$1","c",{"children":[[["$","script",null,{"type":"application/ld+json","dangerouslySetInnerHTML":{"__html":"{\"@context\":\"https://schema.org\",\"@type\":\"BreadcrumbList\",\"itemListElement\":[{\"@type\":\"ListItem\",\"position\":1,\"name\":\"首页\",\"item\":\"https://www.lxpavilion.top/\"},{\"@type\":\"ListItem\",\"position\":2,\"name\":\"技术文章\",\"item\":\"https://www.lxpavilion.top/article\"},{\"@type\":\"ListItem\",\"position\":3,\"name\":\"Docker Desktop 内置 K8s 从入门到实战：部署你的第一个 Nginx 集群\",\"item\":\"https://www.lxpavilion.top/article/63\"}]}"}}],["$","$L2",null,{"params":"$@3","articleTitle":"Docker Desktop 内置 K8s 从入门到实战：部署你的第一个 Nginx 集群","initialContent":"$4"}]],["$L5","$L6","$L7"],"$L8"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"ikq7S5o0Qh3EiXLt_LYoe"}
3:{"id":"63"}
5:["$","script","script-0",{"src":"/_next/static/chunks/0ak3whllmln1c.js","async":true}]
6:["$","script","script-1",{"src":"/_next/static/chunks/116yp~mrtmvyn.js","async":true}]
7:["$","script","script-2",{"src":"/_next/static/chunks/05hxwoutgi7z8.js","async":true}]
8:["$","$L9",null,{"children":["$","$a",null,{"name":"Next.MetadataOutlet","children":"$@b"}]}]
b:null
